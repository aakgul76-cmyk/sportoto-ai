name: Spor Toto verilerini guncelle

on:
  workflow_dispatch:
  push:
    paths:
      - "data/coupon.csv"
      - "data/predictions.csv"
      - "data/consensus.csv"
      - "data/external_predictions.csv"
      - "scripts/fetch_data.py"
      - "scripts/apply_decision_policy.py"
      - "scripts/aggregate_external_predictions.py"
      - "scripts/evaluate_results.py"
      - "scripts/weekly_cycle.py"
      - ".github/workflows/update-data.yml"
      - "docs/index.html"
      - "docs/DECISION_POLICY.md"
  schedule:
    # Her sabah tek yayın: salı-cuma tahmin, cumartesi-pazartesi sonuç değerlendirme.
    - cron: "0 8 * * *"
      timezone: "Europe/Istanbul"

permissions:
  contents: write
  pages: write
  id-token: write

# Veri saglayici anahtari repository genelinde ortaktir. Tum dal ve manuel
# calistirmalari siraya alarak ayni anda API istegi gonderilmesini engelle.
concurrency:
  group: sportoto-provider-api
  cancel-in-progress: false

jobs:
  update-and-build:
    # Pazartesi liste botunun push'u modeli hemen tetiklemesin; manuel push'lar calisir.
    if: ${{ github.event_name != 'push' || github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - name: Repository'yi indir
        uses: actions/checkout@v6
      - name: Python'u hazirla
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Kutuphaneleri kur
        run: pip install -r requirements.txt
      - name: Haftalik calisma modunu belirle
        id: cycle
        run: python scripts/weekly_cycle.py
      - name: Gecen haftayi sali sabahi kapat
        if: steps.cycle.outputs.close_previous_week == 'true'
        env:
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
        run: |
          python scripts/evaluate_results.py --archive
          sleep 11
      - name: Saglayici verileriyle kupon modelini hazirla
        if: steps.cycle.outputs.mode == 'prediction'
        env:
          FOOTBALL_DATA_TOKEN: ${{ secrets.FOOTBALL_DATA_TOKEN }}
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
        run: |
          python scripts/fetch_data.py
          python scripts/aggregate_external_predictions.py
          python scripts/apply_decision_policy.py
      - name: Dondurulmus kuponun sonuclarini degerlendir
        if: steps.cycle.outputs.mode == 'evaluation'
        env:
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
        run: python scripts/evaluate_results.py
      - name: Tahminleri degistirmeden mevcut yayini koru
        if: steps.cycle.outputs.mode == 'frozen' || steps.cycle.outputs.mode == 'publish_only'
        run: echo "${{ steps.cycle.outputs.cycle_reason }}"
      - name: Yeni verileri repository'ye kaydet
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data docs/data
          git diff --cached --quiet || git commit -m "Verileri otomatik guncelle"
          git push
      - name: GitHub Pages'i hazirla
        uses: actions/configure-pages@v5
      - name: Site dosyalarini paketle
        uses: actions/upload-pages-artifact@v4
        with:
          path: docs
  deploy:
    if: github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: update-and-build
    steps:
      - name: Siteyi yayinla
        id: deployment
        uses: actions/deploy-pages@v4
